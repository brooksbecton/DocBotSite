import {
  SAVE_PROVERB,
  SAVE_PROVERB_FAIL,
  SAVE_PROVERB_SUCCESS
} from "./../constants/proverbs";

export default function ProverbReducer(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}
